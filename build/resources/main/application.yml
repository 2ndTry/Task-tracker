  server:
    port: 8081

  spring:
    datasource:
      url: jdbc:postgresql://localhost/task_tracker_api
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true